import Spinner from 'react-bootstrap/Spinner';

const SpinnerLoad = () => {
    return (
        <Spinner animation="border" variant="info" style={{ width: '12rem', height: '12rem' }} />
    );
}

export default SpinnerLoad;